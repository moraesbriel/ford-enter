# 6. Peça para o usuário inserir a base e a altura de um retângulo e calcule a área.

base = float(input("Insira a base do retângulo:"))
altura = float(input("Insira a altura do retângulo: "))

print("A área do retângulo é de", base * altura,"m².")